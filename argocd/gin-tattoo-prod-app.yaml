apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gin-tattoo-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: git@github.com:sant125/aws-devops.git
    targetRevision: HEAD
    path: manifests/gin-tattoo-prod

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
