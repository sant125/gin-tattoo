# kubectl create secret generic tattoo-db-credentials --namespace prod \
#   --from-literal=DATABASE_URL="postgres://tattoo_user:<PASSWORD>@tattoo-db-rw.database:5432/tattoo?sslmode=require" \
#   --dry-run=client -o yaml | kubeseal --format yaml > manifests/gin-tattoo-prod/sealed-secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: tattoo-db-credentials
  namespace: prod
spec:
  encryptedData:
    DATABASE_URL: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: tattoo-db-credentials
      namespace: prod
    type: Opaque
