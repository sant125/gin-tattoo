apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gin-tattoo
  namespace: homolog
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - staging.example.com
      secretName: gin-tattoo-homolog-tls
  rules:
    - host: staging.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gin-tattoo
                port:
                  name: http
