# kubectl create secret generic tattoo-db-credentials --namespace database \
#   --from-literal=username=tattoo_user \
#   --from-literal=password=<PASSWORD> \
#   --from-literal=DATABASE_URL="postgres://tattoo_user:<PASSWORD>@tattoo-db-rw.database:5432/tattoo?sslmode=require" \
#   --dry-run=client -o yaml | kubeseal --format yaml > manifests/database/sealed-secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: tattoo-db-credentials
  namespace: database
spec:
  encryptedData:
    username: REPLACE_WITH_KUBESEAL_OUTPUT
    password: REPLACE_WITH_KUBESEAL_OUTPUT
    DATABASE_URL: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: tattoo-db-credentials
      namespace: database
    type: Opaque
