## kube-prometheus-stack — valores customizados para o gin-tattoo
## Helm chart: prometheus-community/kube-prometheus-stack

# ── Prometheus ────────────────────────────────────────────────────────────────
prometheus:
  prometheusSpec:
    retention: 15d
    retentionSize: "8GB"
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: gp3
          resources:
            requests:
              storage: 10Gi

    # Coleta ServiceMonitors de todos os namespaces
    serviceMonitorSelectorNilUsesHelmValues: false
    serviceMonitorSelector: {}
    serviceMonitorNamespaceSelector: {}

    # Coleta PodMonitors de todos os namespaces (CloudNativePG usa PodMonitor)
    podMonitorSelectorNilUsesHelmValues: false
    podMonitorSelector: {}
    podMonitorNamespaceSelector: {}

    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 1000m
        memory: 2Gi

# ── Grafana ───────────────────────────────────────────────────────────────────
grafana:
  enabled: true
  adminPassword: "${GRAFANA_ADMIN_PASSWORD}"  # injetar via SealedSecret

  # RWO PVC não suporta RollingUpdate — dois pods não podem montar o mesmo volume
  deploymentStrategy:
    type: Recreate

  persistence:
    enabled: true
    storageClassName: gp3
    size: 5Gi

  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

  # Sidecar carrega dashboards de ConfigMaps com label grafana_dashboard="1"
  sidecar:
    dashboards:
      enabled: true
      searchNamespace: ALL
      label: grafana_dashboard
      labelValue: "1"
    datasources:
      enabled: true

  ingress:
    enabled: false

  additionalDataSources:
    - name: Loki
      type: loki
      url: http://loki.observability.svc.cluster.local:3100
      access: proxy
      isDefault: false


# ── AlertManager ─────────────────────────────────────────────────────────────
alertmanager:
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: gp3
          resources:
            requests:
              storage: 2Gi

# ── Exporters ─────────────────────────────────────────────────────────────────
nodeExporter:
  enabled: true

kubeStateMetrics:
  enabled: true

# ── Componentes gerenciados pelo EKS — não acessíveis para scrape ────────────
# No EKS, control plane (etcd, scheduler, controller-manager) roda na conta AWS
# e não expõe endpoints de métricas para o cluster do usuário.
# kubeProxy roda como DaemonSet nos nós EC2 do cluster (não no control plane)
# e é acessível — targets confirmados 3/3 UP no Prometheus.
kubeEtcd:
  enabled: false

kubeControllerManager:
  enabled: false

kubeScheduler:
  enabled: false

kubeProxy:
  enabled: true
